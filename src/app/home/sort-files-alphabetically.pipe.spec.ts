import { SortFilesAlphabeticallyPipe } from './sort-files-alphabetically.pipe';

describe('SortFilesAlphabeticallyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFilesAlphabeticallyPipe();
    expect(pipe).toBeTruthy();
  });
});
